"""Generic image-asset generation via the Replicate HTTP API.

A small, dependency-light toolkit (requests + Pillow, both already project deps)
for producing game/UI image assets from a declarative JSON manifest. Not tied to
Django — runnable as a standalone script.

See README.md in this folder for usage.
"""
